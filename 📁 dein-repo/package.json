{
  "name": "stellify",
  "version": "1.0.0",
  "description": "Stellify – AI Career Copilot Schweiz",
  "private": true,
  "scripts": {
    "build": "npx esbuild stellify-v11-final.jsx --bundle --minify --outfile=stellify-bundle.js --jsx=automatic --define:process.env.NODE_ENV='\"production\"' --target=es2020",
    "dev": "npx serve .",
    "start": "npx serve ."
  },
  "dependencies": {},
  "devDependencies": {
    "esbuild": "^0.20.2",
    "serve": "^14.0.0"
  },
  "homepage": ".",
  "engines": {
    "node": ">=18"
  }
}
